import { renderToVoiceSkill, renderToAlexa } from './renderer';

const Card = 'CARD';
const IntentHandler = 'INTENTHANDLER';
const Response = 'RESPONSE';
const Speak = 'SPEAK';
const VoiceSkill = 'VOICESKILL';

export {
    Card,
    IntentHandler,
    renderToAlexa,
    renderToVoiceSkill,
    Response,
    Speak,
    VoiceSkill,
};
